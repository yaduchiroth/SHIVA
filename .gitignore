node_modules/
.next/
out/
build/
dist/

# Vendored at install time by scripts/fetch-assets.mjs — never commit binaries.
public/mediapipe/
public/models/

.env
.env.local
.env*.local

npm-debug.log*
yarn-error.log*
*.tsbuildinfo
next-env.d.ts

.DS_Store
.vercel

test-results/
playwright-report/
blob-report/
.playwright/

# Deploy archive — contains .env.local when built with credentials.
shiva-deploy.tar.gz

# ─── The mind ───────────────────────────────────────────────────────────────
# Personal data: memories, face embeddings, voiceprints, downloaded models.
# None of it can be regenerated and none of it belongs in a repository.
mind/data/*
!mind/data/demo/
mind/.venv/
mind/.env
__pycache__/
*.pyc
